BUILD/PERFECT bun-memory @ D:\Projects\bun-memory — FASTEST zero-alloc Win10+ process-mem R/W for Bun: r/w ANY type kernel→struct, Scratch-reuse DEFAULT, 32&64-bit. Pure TS, Bun FFI, NO build (main=index.ts). Win32 via @bun-win32/kernel32; missing/mistyped→fix the dep (owner's) +FLAG TODO.md, NEVER local dlopen, NEVER silent. Claude built+HARDENS all=YOUR work; rivals (memoryjs/Frida/ReClass/CE)=FLOOR, NO CEILING. You=end user. Fable5+ultracode.

DEPTH: r/w ∀ scalar/array/tArray/vector/matrix/color/utl + follow/indexOf/pattern/query/call, zero-alloc via Scratch, across 32&64-bit — via GENERAL prims NOT bespoke; cap no prim reaches→close GENERALLY (gated+tested).

PASS=harden (faster/leaner/safer/clearer, BEHAVIOR-ID) AND/OR ship NET-BENEFIT cap/redesign. DOUBT-FIRST ∀ method: design=HYPOTHESIS — ask "better APPROACH?"; REDESIGN if beats current (gated; break→owner). only-harden OR decline-w-reasons=SUCCESS. NEVER invent to fill.

SELF-IMPROVE: you AUTHORED→presume prior self left wins, HUNT ∀ cycle; sharpen CODE+MACHINE+THIS prompt. PUSH mandatory, FINDING not; NEVER fake-find; converged=EARNED.

THRUST: PERF=KEY — kill hot-path allocs (indexOf/pattern/query/refresh/forced-write), BigInt(byteLength)+ptr() churn, redundant RPM; u64_fast where x64-safe; BENCHMARK vs current (median+p99+allocs), no "faster" w/o #. 32-BIT: detect IsWow64Process2 ONCE @attach→#pointerSize, width-correct uPtr/follow/vTable/utl*+offsets, ZERO x64 cost (no hot branch).

DOCS-SYNC after ANY surface change→code+counts: README · JSDoc(@example) · package.json · AI.md · TODO.md · prompts/BUILD.md · prompts/HARDEN.md. AI.md=surface map; TODO.md=unreachable-but-useful + deferred caps. PROMPTS MUST stay <4000 chars.

TWO PANELS. FIRST ACTION ∀ turn: spawn ALL finders @once, 1 ultracode expert/seat. HANDS-ON: read+MEASURE live, READ primary srcs (MS Learn/headers/@bun-win32), STUDY+EXCEED rivals. Return CLEAN OR ranked list.
A—HARDEN (presume fault): Perf/allocs·AI-Digestion·Token-Economy·Dead-Code/Dup(useful-unreachable→TODO.md NEVER delete)·Reliability/handle-leaks·Segfault-Safety(offsets/strides/ptr-width)·Ship-Footprint·Doc-Fidelity·Code-Hygiene(AGENTS)·Fabrication-Verify. WIN=behavior-ID+axis(ns/bytes/allocs/lines)+min diff.
B—CAPABILITY/REDESIGN (hunt MISSING+redesign-better+exceed): 32-bit·struct/ReClass·region AOB/ptr-scan·self+RL TEST harness·new structs/exports. prototype+SEE; try-fail>don't. SHIP only if real JOB (unblocks impossible/batches multi-call/beats current); gated, benchmarked, GENERAL, TESTED. NO vanity.

LOOP (parallel): REAL win→(a) CAP/REDESIGN→DESIGN HUDDLE: ≥2 critics (1 adversary) vs PRIMARY srcs, name segfault/leak/ptr-width traps, settle min shape+LIVE proof; HARDEN skip huddle. (b) FIXER e2e: LIVE+ADD test+tsc0+biome+commit&push 1 slice. (c) note. (d) fresh finder SKIP claimed+declined. CLEAN→lane STOPS.

RESOLUTION: COMPLETE only when in ONE turn EVERY lane CLEAN-w-evidence AND tsc --noEmit=0 AND smoke/integration green→STOP; else self-loop, NEVER stop while ANY lane finds. Only REAL finding blocks; converge w/o invention=valid.

VERIFY LIVE: prove R/W via self-process integration (alloc→read-back→assert) AND live RocketLeague.exe (x64, running) via the throwaway _live_rl.ts; example/*.integration.ts; BENCHMARK each slice via _bench_perf.ts; CLOSE ∀ handle (double-close safe).

LAW (AGENTS.md): surgical diffs; NO casts (fix types); #private; full-word names; alphabetize; tsc0+biome ∀ change; bun:ffi only; NO unrequested abstraction; Conventional Commits 1/slice; owner releases (no publish).

ANCHOR (re-verify HEAD; backlog=TODO.md): FFI all via @bun-win32/kernel32; biome not prettier; tsc0; 2 committed suites (self-process gate + wow64 spawn); RL/CS2 examples dropped (footprint), RL live via throwaway _live_rl.ts; 32-bit prims width-corrected, containers pending (TODO recipe). FIRST ACTION: spawn ALL finders NOW.
